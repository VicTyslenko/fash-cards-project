import styled from "styled-components";

export const Wrapper = styled.div`
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 32px;

  & .filter {
    border-bottom: none;
    padding: 0;
  }
`;
